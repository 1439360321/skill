1.采用专项prompt
2.微调模型
3.多agent协作（和1差不多）
4.调用LLM AGENT 工具 
5。让ai使用Chain-of-Thought 思维链
微调 temperature 产生不同思路，然后多次回答进行投票
6.三条路径融合
路径1：控制路径
    │     AST + CFG + DFG → 文本化 → "函数X调用Y，参数Z来自输入"
    │
    ├─→ 路径2：知识路径
    │     混合稠密-稀疏检索 → CWE知识库 → 相似漏洞案例
    │
    ├─→ 路径3：语义路径
    │     代码功能摘要 → "这个函数做文件备份操作"
    │
    └─→ 三条融合 → LLM一次推理 → 判断
    不太懂
7.没看懂
8.更改输入LLM的格式
上下文稀释效应，源码加太多llm的注意力下降

优化点：
尝试使用微调后模型（2）
输入结构化  （8）
使用思维链+多检测投票  （5）
多agent协作，采用专项prompt （1）（3）
(初筛假阳性) 验证真漏洞 关联证据 汇总+冲突解决